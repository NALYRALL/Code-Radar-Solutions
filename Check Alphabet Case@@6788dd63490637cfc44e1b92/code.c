#include<stdio.h>

int main(void)
{
    char a;
    scanf("%c",&a);
    if (a<=z || a>=a)
    {
        printf("Lowercase");
    }
    else if (A<=Z || A>=A)
    {
        printf("Uppercase");
    }
    else 
    {
        printf("");
    }
    return 0;
}